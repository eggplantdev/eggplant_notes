// DOM id of the /memory-cards in-place review panel, shared by the panel wrapper and the per-card
// Review button (which smooth-scrolls to it). One source so the anchor can't drift.
export const REVIEW_PANEL_ID = 'review-panel'
