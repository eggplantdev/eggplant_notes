# CLAUDE.md — Claude Code entry point

**Everything lives in @AGENTS.md** — the canonical, complete onboarding file. The `@AGENTS.md` reference above is a Claude Code import, so its full content is inlined here automatically; this file has no content of its own.

> Note: `10x get` may re-stamp a `<!-- BEGIN @przeprogramowani/10x-cli -->` lesson block below this line on the next fetch. It's a tool-injected digest whose rules already live in `@AGENTS.md` (and the relevant skill) — not a second source. Safe to delete again; ignore it as canon.
