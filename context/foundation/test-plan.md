# Test Plan

> Phased test rollout for this project. Strategy is frozen at the top
> (§1–§5); cookbook patterns at the bottom (§6) fill in as phases ship.
> Read before writing any new test.
>
> Refresh: re-run `/10x-test-plan --refresh` when stale (see §8).
>
> Last updated: 2026-06-10

## 1. Strategy

Tests follow four non-negotiable principles for this project:

1. **Cost × signal.** The cheapest test that gives a real signal for the
   risk wins. Do not promote to e2e because e2e "feels safer." Do not put a
   vision model on top of a deterministic visual diff that already catches
   the regression.
2. **User concerns are first-class evidence.** Risks anchored in "the team
   is worried about X, and the failure would surface somewhere in <area>"
   carry the same weight as PRD lines or hot-spot data.
3. **Risks are scenarios, not code locations.** This plan documents _what
   could fail_ and _why we believe it's likely_ — drawn from documents,
   interview, and codebase _signal_ (churn, structure, test base). It does
   NOT claim to know which line owns the failure. That knowledge is
   produced by `/10x-research` during each rollout phase. If the plan and
   research disagree about where the failure lives, research is the
   ground truth.
4. **Assert the effect, not the 200.** A test must assert the observable or
   persisted result of a scenario — the rescheduled due-date, the written
   row, the denied access — never the fact that an action "returned
   success." A returned 200 (or a green optimistic UI) is not proof a
   side-effect happened; a swallowed error can return success while the real
   work failed. The oracle for the expected effect comes from the risk /
   requirement, never from the code under test.

Hot-spot scope used for likelihood weighting: `src/`, `supabase/` (hand-written only; excludes docs, fixtures, archive, build output, and 10x-cli-managed files).

## 2. Risk Map

The top failure scenarios this project must protect against, ordered by
risk = impact × likelihood. Risks are failure scenarios in user / business
terms, not test names. The Source column cites the _evidence that surfaced
this risk_ — never a specific file as "where the failure lives" (that is
research's job, see §1 principle #3).

| #   | Risk (failure scenario)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Impact | Likelihood | Source (evidence — not anchor)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | A user reads or writes **another user's** notes, cards, subjects, settings, or review events — an RLS gap on a table or an RPC running at the wrong privilege                                                                                                                                                                                                                                                                                                                                                                                                     | High   | Med        | PRD v2 Guardrails (isolation #1); interview Q1 + Q4; hot-spot dirs (refreshed 2026-06-10) `src/features/memory-cards/` (198/30d), `src/features/subjects/` (108/30d), `src/features/auth/` (42/30d), `src/features/api-tokens/` (28/30d). **The `/api/*` token surface is R1 by convergence** — same RLS + minted-JWT (`resolve_api_token` DEFINER) ownership boundary, just a Bearer-token entry instead of a session cookie; its IDOR is covered by `api-tokens.integration` / `api-routes.integration` (default-skipped, see §4)                                                                                                                                                                                                                                                                                                              |
| 2   | After a self-rating, a card **reschedules wrong or drops out of the due set** — FSRS state corrupts silently and the recall loop stops surfacing the right cards                                                                                                                                                                                                                                                                                                                                                                                                  | High   | Med        | PRD v2 Guardrails (recall loop #2); roadmap north-star S-03; hot-spot dirs (refreshed 2026-06-10) `src/features/review/` (72/30d), `src/features/review-events/` (29/30d)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | **AI-generated notes/cards commit without schema-validation or the preview gate** — malformed or hallucinated rows written to the user's data                                                                                                                                                                                                                                                                                                                                                                                                                     | High   | Med        | interview Q3 + Q4; roadmap S-19 P2 + slices `ai-generation-robustness`/`editable-system-prompts`/`note-create-ai-cards` (archived 2026-06-08); hot-spot `src/features/openrouter/` (156/30d). **Now substantially unit-tested** (2026-06-10): `ai-schemas`, `sanitize-generated`, `prompts` (preview≡builder), `notes-schema`, `card-schema`, `user-prompts`, `describe-generation-error` + E2E `create-note-with-checks` — Phase 4's schema/gate half is effectively done                                                                                                                                                                                                                                                                                                                                                                       |
| 4   | **Uncontrolled token spend** — an AI generation runs with no size or budget ceiling; a large file or a repeated trigger burns the user's OpenRouter credits unboundedly (resource abuse)                                                                                                                                                                                                                                                                                                                                                                          | High   | Med        | interview Q2 (explicit top fear); roadmap S-19 P2; hot-spot `src/features/openrouter/` (156/30d). **2026-06-10 grounding:** a per-request size ceiling EXISTS (`generate-notes.ts`/`generate-cards.ts` Zod `.max()`: 50k chars, 10 MB PDF, 100k-char prompt) and the **over-limit refusal is now unit-tested** (`generate-caps.test.ts`, 2026-06-10 — notes text/topic/PDF + cards topic/draft, each asserts refusal + `generateObject` never called, mutation-checked against a widened cap). What remains is a pure **code** gap: there is **NO call-count / repeat-trigger guard anywhere in code** — so "cannot be looped to bypass it" is unmet by **code** (nothing to test until a rate limiter is built). Mitigation: BYOK — a loop burns the user's _own_ OpenRouter credits (OpenRouter caps them); no shared provider budget to drain |
| 5   | **OpenRouter credential leaks** into logs, error bodies, or the client bundle, or **survives account deletion** (FR-006 requires its removal)                                                                                                                                                                                                                                                                                                                                                                                                                     | High   | Med        | interview Q4; roadmap S-19 connect; PRD v2 Access Control + S-05 delete-account (built + archived 2026-06-03)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 6   | A Server Action mutation **fails but the UI shows success** (the `reorderNote` silent-revert class) — data loss with no user-visible signal                                                                                                                                                                                                                                                                                                                                                                                                                       | Med    | Low        | roadmap S-16 motivation (class closed; residual risk is regression of the signal mechanism)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 7   | **Untrusted markdown runs script in the user's session** — an imported `.md` or an AI-generated note body renders raw HTML (`<script>`, `on*=`) or a `javascript:`/`data:` URL that executes in the authenticated session (JWT/session theft, acting as the user). Protection rests on react-markdown defaults (no `rehype-raw`, default `urlTransform`), now guarded by `e2e/markdown-xss.spec.ts` (user-editor + `.md`-import paths; AI generation + token HTTP API covered-by-convergence — all four sources write through the same `RenderMarkdown` pipeline) | High   | Med        | operator's explicit worry (malicious scripts via markdown inputs); **D.1** — user-authored note bodies are a live, **non-AI** injection surface today (`context/changes/test-suite-audit/test-suite-audit.md` §D), not only the R3 AI path; `.md` import (archived 2026-06-06) + AI bodies (R3, archived 2026-06-07) are the other untrusted sources                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8   | **Subject/note ordering corrupts** — fractional-position reordering (`reorder-note.ts`) degenerates (collisions / precision loss) so notes render in the wrong order or an order that cannot be re-saved                                                                                                                                                                                                                                                                                                                                                          | Med    | Low        | **D.2** audit backport (`context/changes/test-suite-audit/test-suite-audit.md` §D; `context/archive/2026-06-04-subject-sidebar-nav/research.md:50`); high-churn `src/features/subjects/`. **2026-06-10:** the fractional-position math IS now unit-covered (`midpoint.test.ts`, shared by `reorder-note.ts`); the **e2e reorder-degeneracy path** (collisions / precision loss / un-re-savable order) remains uncovered, logged for a future phase                                                                                                                                                                                                                                                                                                                                                                                               |

**Impact × Likelihood rubric.** High impact = user loses access, data, or money. High likelihood = area changes weekly or we have already been burned here. R3–R4 are scored Medium likelihood; the S-19 surface they ride on is **now built + archived** (2026-06-07). R5 rides on S-19 _connect_ + S-05 delete-account (archived 2026-06-03). All three risks are therefore active — their §3 test phases (4–5) remain to be done (see §8 refresh trigger).

**Abuse / security lens.** The product has auth and accepts user input (markdown upload), and will hold a BYOK secret. Coverage: authorization/IDOR → R1; untrusted input / validation parity → R3; secret/PII leakage → R5; resource abuse → R4.

**Security-hardening pass (2026-06-10) — landed by parallel agents as standalone commits (no change folder).** Five findings were fixed on the auth / account / error surfaces these risks cover. They were **code fixes, not tests**. **2026-06-10 refresh — 3 of 5 now have regression tests**, so the original "hardened but still untested" claim is partly stale; (1) and (2) remain hardened-but-untested:

- (1) sign-up errors collapsed to a neutral message — no user enumeration (`7f56b2b`) → R1 / auth-abuse. **Still untested** (no spec asserts the collapsed message; `auth-validate.test.ts` covers the schema only).
- (2) `runTableAction` returns a generic error instead of the raw PostgREST message — no schema/constraint leak to the client (`e82c5b5`) → **R5 error-leak surface** (the token-API `*-core` path has its own contract + tests and was intentionally left untouched). **Still untested** (no spec imports `runTableAction`; fix verified in code only).
- (3) account deletion now requires password re-auth (`af5cab3`) → R5 / S-05 account surface (`update-password` left recovery-only by design — the email link is itself the re-auth). ✅ **Covered** by `e2e/delete-account.spec.ts` (confirm disabled until DELETE typed + password re-entered).
- (4) minimum password length 6 → 8, NIST baseline, app + Supabase config in sync (`b0c4120`) → auth. ✅ **Covered** by `auth-validate.test.ts` ("rejects a password shorter than 8 characters").
- (5) contact email subject rejects CR/LF — header-injection defense (`5814a55`) → untrusted-input / injection lens. ✅ **Covered** by `contact-schema.test.ts` ("rejects a subject with a newline").

### Risk Response Guidance

| Risk | What would prove protection                                                                                                                                                                                               | Must challenge                                                                                                                                                                                        | Context `/10x-research` must ground                                                                                                                                                                                                                                                 | Likely cheapest layer                                                                                                           | Anti-pattern to avoid                                                                                                                                                                                    |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #1   | A non-owner is **denied** both read and write on every table, and every RPC runs at the intended privilege (invoker vs definer)                                                                                           | "Owner-can-read-own passes" does **not** imply "non-owner is denied"                                                                                                                                  | Each table's RLS policy set; every RPC's `SECURITY` mode; which client (RLS-scoped vs service-role) each read path uses                                                                                                                                                             | integration (two-user DB) + extend e2e `isolation.spec.ts`                                                                      | Testing only the happy owner path and never the cross-user denial                                                                                                                                        |
| #2   | Rating Good lengthens / Again shortens the next-due interval, and the card correctly leaves and re-enters the due set across day boundaries                                                                               | "The action returned success" does **not** imply "the FSRS state rescheduled correctly"                                                                                                               | The rating→FSRS-rating mapping; the due-query predicate and its timezone handling; what a review event persists                                                                                                                                                                     | unit (mapping) + integration (due-query + reschedule)                                                                           | **Oracle problem** — asserting the expected interval by copying FSRS's own output (tautological); the oracle is FSRS direction semantics, not the code under test                                        |
| #3   | Every AI-produced note/card passes schema validation and the user preview/edit gate **before** any DB write                                                                                                               | "The model returned JSON" does **not** imply "the JSON is a valid, safe row"                                                                                                                          | The structured-output schema; where validation runs; whether commit is reachable without passing the preview gate                                                                                                                                                                   | unit (schema) + integration (commit path)                                                                                       | "Test the AI" — assert the _contract and gate behavior_, never the model's wording                                                                                                                       |
| #4   | A generation request refuses or truncates past a defined token/size ceiling, and cannot be looped to bypass it                                                                                                            | "Users won't upload huge files" / "one call is cheap" — cost compounds on repeat                                                                                                                      | Where the ceiling is enforced; what counts toward it (input size, output tokens, call count)                                                                                                                                                                                        | integration on the generation entry point                                                                                       | Testing only a small happy-path input and never the over-limit refusal                                                                                                                                   |
| #5   | The key never appears in logs, error responses, or the client bundle, and is gone after account deletion                                                                                                                  | "It saves correctly" does **not** imply "it is absent from error paths" or "it is removed on delete"                                                                                                  | Where the key is stored and how it is encrypted; the delete-account cascade; what error/log paths can echo it                                                                                                                                                                       | integration (storage + delete) + response/bundle scan                                                                           | Testing only that the key saves, never that it is absent on error or removed on delete                                                                                                                   |
| #6   | A failing mutation surfaces a user-visible signal (toast or inline error) rather than a silent optimistic revert                                                                                                          | "The optimistic UI updated" does **not** imply "the write succeeded"                                                                                                                                  | The action-result plumbing that converts a failure into a surfaced signal                                                                                                                                                                                                           | one thin e2e on a representative failing action                                                                                 | Testing toast styling/copy/animation (that is §7 negative space)                                                                                                                                         |
| #7   | A note whose body carries `<script>`, an `on*=` handler, and a `javascript:`/`data:text/html` link renders **inert** — no script runs, no element/handler is injected into the DOM, and the dangerous href is neutralized | "We don't use `rehype-raw`, so we're safe" does **not** imply a test proves it — a future plugin, `urlTransform` override, or custom `a`/`img` component can re-enable execution with no failing test | Every remark/rehype plugin in the pipeline (incl. the new `markdown-plugins.ts`) and whether any admits raw HTML; the `urlTransform` default + any override; whether a custom link/image component reads `href`/`src` raw; every untrusted source feeding the renderer (import, AI) | e2e (paste & observe): create a note with the payload, assert no execution + inert DOM; extend to the import path when it lands | Asserting only "no alert dialog appeared" (a payload that fails to fire for an unrelated reason still passes), or testing react-markdown's internal escaping instead of our pipeline's observable output |

## 3. Phased Rollout

Each row is a discrete rollout phase that will open its own change folder
via `/10x-new`. Status moves left-to-right through the values below; the
orchestrator updates Status as artifacts appear on disk.

| #   | Phase name                         | Goal (one line)                                                                                                   | Risks covered                  | Test types                                                       | Status      | Change folder                                                  |
| --- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------- | ----------- | -------------------------------------------------------------- |
| 1   | RLS isolation depth                | Prove no cross-user read/write on any table and every RPC at the right privilege                                  | #1                             | integration + e2e                                                | not started | —                                                              |
| 2   | Recall-loop scheduling integrity   | Prove reschedule direction and due-set membership are correct after a rating                                      | #2                             | unit + integration                                               | not started | —                                                              |
| 3   | Mutation-feedback regression guard | Prove no Server Action mutation can fail silently                                                                 | #6                             | e2e                                                              | not started | —                                                              |
| 4   | AI generation safety               | Prove AI output is schema-validated, preview-gated, and bounded by a token/size ceiling                           | #3, #4                         | unit + integration; Playwright MCP exploratory on the preview UI | not started | —                                                              |
| 5   | OpenRouter credential lifecycle    | Prove the BYOK key never leaks and is removed on account deletion                                                 | #5                             | integration + response/bundle scan                               | not started | —                                                              |
| 6   | Memory-card filter surface         | Prove state/maturity/subject filters narrow server-side, AND-compose, and deep-link pre-filtered                  | regression (not a §2 top risk) | e2e                                                              | complete    | context/archive/2026-06-06-memory-card-state-maturity-filters/ |
| 7   | Markdown render XSS guard          | Prove untrusted markdown (imported file, AI body) renders inert — no script execution, dangerous URLs neutralized | #7 (complements #3)            | e2e (paste & observe)                                            | complete    | context/archive/2026-06-09-testing-markdown-xss-guard/         |

**Status vocabulary** (fixed — parser literals): `not started` → `change opened` → `researched` → `planned` → `implementing` → `complete`.

Phases 1–3 are testable against today's code. Phases 4–5 cover the AI generation + credential surface, which **has now landed** (S-19, archived 2026-06-07) — they remain `not started` as **dedicated rollout phases** (no per-phase change folder exists yet — the status literal is kept for the parser) but are **no longer gated**; the `test-plan-refresh-2026-06-10` change (opened via `/10x-test-plan --refresh`) re-grounds them (R4-first per the operator interview). **See the Incidental-coverage note below — Phases 1–5 each already carry meaningful coverage even though no dedicated phase has been opened.** This ordering was deliberate: the operator's top fears lived in then-unbuilt code, so the plan named them early and attacks them now that the code exists. **Note:** the parallel **2026-06-10 security-hardening pass** (see §2 Abuse / security lens) already hardened parts of the R5 error-leak + account-delete surface — Phase 5's tests target the current hardened state, and several of those fixes are regression-test candidates.

Phase 6 is **out-of-band regression coverage**, registered post-hoc for an already-shipped feature (`memory-card-state-maturity-filters`, committed d822d67 + 34fd528). It does not map to a §2 top risk — it is a read-side correctness guard for the filter surface. Its E2E (`e2e/memory-card-filters.spec.ts`) is authored as that change's Phase 3, **after** its `/simplify` pass per the per-slice review gate.

Phase 7 (markdown render XSS) is a **render-layer** guard, deliberately distinct from Phase 4: Phase 4 / R3 validate AI _output_ against a schema and the preview gate at **write time**, whereas #7 proves the **render** pipeline neutralizes dangerous markup from **any** source — a schema-valid note body can still carry `<script>`. It now covers the **user-editor** and **`.md`-import** paths directly via `e2e/markdown-xss.spec.ts`, with **AI-generated bodies** and the **token HTTP API** covered **by convergence** — all four untrusted sources write through the same `RenderMarkdown` pipeline, so no separate per-source spec is needed. Memory-card fields render through the same pipeline (also by convergence).

**Incidental-coverage note (2026-06-10 refresh).** No dedicated rollout change folder has been opened for Phases 1–5, so their §3 Status stays `not started` — but the suite that grew alongside feature work already covers much of each. Grounded in `context/changes/test-plan-refresh-2026-06-10/research.md`:

| Phase | Risk   | Incidental coverage today                                                                                                        | Remaining gap                                                                                                              |
| ----- | ------ | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 1     | R1     | E2E `isolation.spec.ts` + per-feature `LEAK:` checks (memory-cards/subjects/review); `api-*.integration` IDOR                    | a dedicated two-user RLS-per-table integration sweep; **API IDOR specs are default-skipped** (not in the `pnpm test` gate) |
| 2     | R2     | unit `review-scheduling` + E2E `review` / `memory-card-review-page`                                                              | integration due-query + timezone/day-boundary membership                                                                   |
| 3     | R6     | unit `toast-result` + E2E `action-feedback-toasts`                                                                               | the imperative reorder-failure seam is verified manually, not automated                                                    |
| 4     | R3, R4 | **R3 mostly done** (7 unit specs + `create-note-with-checks`); **R4 per-request caps now unit-tested** (`generate-caps.test.ts`) | **R4 has no repeat/loop guard in code** — a code gap, not a test gap (see §2 R4)                                           |
| 5     | R5     | `aes-gcm` (encryption-at-rest) + `delete-account.spec` (delete flow + re-auth)                                                   | no assertion the credential row is gone after delete; no error-body/client-bundle leak scan                                |

The cheapest next moves the refresh surfaced: the R4 over-limit-refusal test (**done 2026-06-10** — `generate-caps.test.ts`); still open — an R5 "credential row gone after delete" assertion + a response/bundle leak scan, and a regression test for hardening fixes (1)/(2).

## 4. Stack

The classic test base for this project. AI-native tools carry a `checked:`
date so future readers can see which lines need re-verification.

| Layer                | Tool                                          | Version | Notes                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | --------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| unit + integration   | Vitest                                        | ^4.0.18 | 40 specs in `src/__tests__/` (versions re-verified 2026-06-10); pure-logic + schema + write-path-core coverage. **2 are `RUN_INTEGRATION`-gated** (`api-routes.integration`, `api-tokens.integration`) — they hit real route handlers + the local Supabase stack and run **only** via `pnpm test:integration`, so the default `pnpm test` gate does NOT exercise API-level IDOR/route handlers |
| e2e                  | Playwright                                    | ^1.60.0 | 22 specs in `e2e/`; system Chrome, production build on port 3100, no DB reset (self-seeding via real sign-up)                                                                                                                                                                                                                                                                                  |
| API mocking          | none yet                                      | —       | No MSW. Integration tests run against the local Supabase stack, not mocked HTTP                                                                                                                                                                                                                                                                                                                |
| accessibility        | none yet                                      | —       | No axe-core wired; not gating any current risk                                                                                                                                                                                                                                                                                                                                                 |
| scheduling engine    | ts-fsrs                                       | ^5.4.1  | Library under integration (R2) — test our integration, not its internal math (§7)                                                                                                                                                                                                                                                                                                              |
| (optional) AI-native | Playwright MCP — checked: 2026-06-06          | n/a     | Exploratory/multimodal e2e on the AI preview UI (Phase 4). When NOT to use: any flow a deterministic schema/integration test already covers cheaply                                                                                                                                                                                                                                            |
| mutation gate        | Stryker + vitest-runner — checked: 2026-06-07 | ^9.6.1  | Ad-hoc, not CI. Drives the vitest runner only, so E2E coverage is invisible to it; `mutate` glob in `stryker.config.json` is scoped to unit-covered pure-logic modules. `pnpm install` then `npx stryker run` (or `--mutate <file>` per risk)                                                                                                                                                  |

**Stack grounding tools (current session):**

- Docs: Context7 — available; use for current Next.js 16 / Playwright / ts-fsrs / Supabase APIs at per-phase planning; checked: 2026-06-06
- Search: Exa.ai — available; use for tool-currency checks (e.g. OpenRouter PKCE contract, token-counting libs) at Phase 4/5 planning; checked: 2026-06-06
- Runtime/browser: Playwright MCP — available; already the e2e surface; candidate exploratory layer for the AI preview UI; checked: 2026-06-06
- Provider/platform: Linear + Supabase MCPs — available; Supabase MCP can verify RLS policies/RPC privilege for Phase 1 gates; Linear mirrors rollout issues; checked: 2026-06-06

## 5. Quality Gates

The full set of gates that must pass before a change reaches production.
"Required after §3 Phase N" means the gate is enforced once that rollout
phase lands; before that, the gate is `planned`.

| Gate                                                 | Where                             | Required?                         | Catches                                                                                       |
| ---------------------------------------------------- | --------------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------- |
| lint + typecheck                                     | local (pre-commit) + Vercel build | required                          | syntactic / type drift                                                                        |
| unit + integration                                   | local + CI                        | required after §3 Phase 1         | logic + isolation + scheduling regressions                                                    |
| e2e on critical flows (auth, isolation, recall loop) | local + CI                        | required after §3 Phase 1         | broken critical user paths                                                                    |
| post-edit hook                                       | local (agent loop)                | recommended (configured in M3 L3) | regressions at edit time                                                                      |
| mutation-verification (test proven to fail)          | local on changed files            | recommended after §3 Phase 1      | tautological tests that pass even when the code is broken (assertion mirrors, oracle problem) |
| AI output-contract gate                              | local + CI                        | required after §3 Phase 4         | unvalidated/over-budget AI generations                                                        |
| credential-leak scan                                 | CI                                | required after §3 Phase 5         | secret in logs/responses/bundle                                                               |
| pre-prod manual smoke (incl. sample-data load)       | between merge + prod              | optional                          | environment-specific + demo-path failures                                                     |

CI today is Vercel's GitHub integration (preview on push, prod on merge); there is no `.github/workflows/*`. Gates marked "CI" become enforceable when a CI runner is wired — that wiring is owned by M1 L5 / M2 L5, not this plan.

Mutation-verification is the discipline of confirming a test actually fails when the code it guards is broken (it catches assertion-mirrors and the oracle problem). Scope it to changed files only (e.g. a mutation runner like Stryker on the diff); a full-suite mutation run is too slow to gate on.

**Tooling (wired 2026-06-07):** Stryker + `@stryker-mutator/vitest-runner`, config in `stryker.config.json`. It stays **ad hoc** — run it on a risk-critical module before a merge, never on every commit. Load-bearing constraint: Stryker drives **one** test runner (vitest), so anything covered only by Playwright E2E is invisible to it. A bare `stryker run` over `src/**/*.ts` would report most of the app as `No coverage` — a misleading 0, not a quality signal. The `mutate` glob is therefore scoped to the pure-logic modules unit tests actually cover; per-risk runs narrow further with `--mutate <file>`. Don't chase 100%: survived mutants are a triage list ("would this bug hurt a user?"), and equivalent mutants (perf-only, cosmetic) are left consciously. See §6.7.

## 6. Cookbook Patterns

How to add new tests in this project. Each sub-section is filled in once
the relevant rollout phase ships; before that, it reads "TBD — see §3 Phase N."

### 6.1 Adding a unit test

- **Location**: `src/__tests__/<name>.test.ts`.
- **Naming**: `<behavior-or-module>.test.ts` (flat dir, kebab-case).
- **Reference test**: `src/__tests__/review-scheduling.test.ts`.
- **Run locally**: `pnpm test`.

### 6.2 Adding an integration test (RLS / two-user)

- TBD — see §3 Phase 1 (cross-user denial pattern against the local Supabase stack).

### 6.3 Adding an e2e test

- **Location**: `e2e/<feature>.spec.ts`.
- **Self-seeding**: each spec signs up a fresh per-run `uniqueEmail` (`e2e/helpers.ts`); specs do not reset the DB.
- **Reference test**: `e2e/isolation.spec.ts` (two-account), `e2e/review.spec.ts` (recall loop).
- **URL-multiselect filters** (subject/state/maturity): `e2e/memory-card-filters.spec.ts` — target via `data-testid="filter-<key>"`, assert list narrowing + AND composition (authored in `memory-card-state-maturity-filters` Phase 3).
- **Render-security guard** (XSS / inert markdown): `e2e/markdown-xss.spec.ts` — fire raw-HTML + dangerous-URL payloads through a real ingestion path, assert inert DOM (`.prose script`/`img` count 0, handler globals falsy) + neutralized `href`; never assert merely "no alert appeared" (R#7 anti-pattern).
- **Run locally**: `pnpm test:e2e` (needs `supabase start` up).

### 6.4 Adding a test for the recall loop

- TBD — see §3 Phase 2 (rating→reschedule direction + due-set membership; oracle from FSRS semantics, not the code).

### 6.5 Adding a test for the AI generation surface

- **Schema / sanitize (exists)**: assert the structured-output contract and runtime emptiness, never the model's wording. Reference: `src/__tests__/ai-schemas.test.ts`, `src/__tests__/sanitize-generated.test.ts`.
- **Preview-gate equivalence (exists)**: prove "what you see is what gets sent" — the previewed prompt ≡ the prompt the action builds. Reference: `src/__tests__/prompts.test.ts`.
- **Token/size ceiling (exists — see §2 R4)**: the per-request caps live as Zod `.max()` in `src/features/openrouter/actions/generate-notes.ts` / `generate-cards.ts` (50k chars, 10 MB PDF, 100k-char prompt) and are now guarded by `src/__tests__/generate-caps.test.ts` — it drives each cap one char over, asserts the refusal AND that `generateObject` is never called (over-limit ⇒ zero tokens). Reference pattern for any new generation cap: mock the heavy deps (`server-only`, `server-client`, `queries`, `notes/queries`, `log-generation`, `ai`) and wire the baseline so a within-cap request _would_ reach the model — that baseline is what makes the over-limit assertion mutation-proof rather than a tautology. There is still **no repeat/loop guard in code** to test (BYOK-mitigated; see §2 R4) — assert the per-request refusal, do not pretend a rate limit exists.

### 6.6 Per-rollout-phase notes

(Optional. After each phase lands, `/10x-implement` appends a 2–3 line note here capturing anything surprising the phase taught.)

### 6.7 Running the mutation gate (Stryker)

- **When**: ad hoc on a risk-critical module before a merge — not per commit.
- **Run**: `pnpm install` (deps live in `package.json`), then `npx stryker run` (whole scoped glob) or `npx stryker run --mutate "src/<path>.ts"` (one risk). Report: `reports/mutation/mutation.html` (gitignored).
- **Scope rule**: only mutate modules a **unit test** covers. Stryker uses the vitest runner; E2E-covered code shows as `No coverage` and drags the score for no reason. Add new pure-logic modules to the `mutate` list in `stryker.config.json`; do **not** widen it to `src/**/*.ts`.
- **Triage, don't chase the score**: for each survived mutant ask "would this bug hurt a user?" Real gap → add a behavioural assertion (not a mirror of the impl). Equivalent/cosmetic (perf-only, message strings, option objects) → leave consciously.

## 7. What We Deliberately Don't Test

Exclusions agreed during the Phase 2 interview (Q5). Future contributors
should respect these unless the underlying assumption changes.

- **Presentational / UI polish** (toast visuals/copy/animation, motion, nav shell, progress-bar glow, celebration dialog) — low blast radius. **Exception:** keep the one e2e (Phase 3) that a failed mutation surfaces _some_ signal — that is the lived `reorderNote` lesson, not decoration. Re-evaluate if a visual regression ever causes data loss. (Source: interview Q5.)
- **Seed / sample-data internals** (`supabase/seed.sql`, `generate-section-seed.mjs`, the per-user remap) — dev-only + graded-demo convenience, not a production path. Verify by **manual smoke before evaluation** instead. Re-evaluate if sample-data becomes a real onboarding feature. (Source: interview Q5.)
- **Markdown render fidelity** (exact Shiki token colors / markdown-to-HTML output via snapshots) — brittle, breaks on every Shiki bump, catches nothing real. **This exclusion is fidelity only — it does NOT cover the markdown render _security_ guard (R#7 / Phase 7, `e2e/markdown-xss.spec.ts`), which is required.** Re-evaluate if token _meaning_ (not color) ever regresses. (Source: interview Q5.)
- **`editable-system-prompts` E2E** (the save → cross-surface reopen → generate-honors-saved-prompt → reset → built-in flow, once on the test-debt list) — **deliberately won't-do.** The prompt is now user-editable free text fed to a live LLM, so "generation honored the saved prompt" has no fixed oracle to assert against; the correctness that matters — _what you see in the preview is exactly what gets sent_ — is already unit-locked by `prompts.test.ts` (preview≡builder) and `user-prompts.test.ts` (resolve/default). Save/Reset/cross-surface reopen is low-blast-radius UI plumbing. Re-evaluate if a saved prompt is ever silently dropped or a built-in leaks across users. (Source: operator decision 2026-06-10.)

**Not** excluded: our _integration_ with third-party libraries (FSRS, Supabase, Shiki). We trust their internals; we test that we call them correctly — especially the recall loop (R2), which is the north star.

## 8. Freshness Ledger

- Strategy (§1–§5) last reviewed: 2026-06-10
- Stack versions last verified: 2026-06-10 (Vitest ^4.0.18, @playwright/test ^1.60.0, ts-fsrs ^5.4.1, Stryker ^9.6.1 — all current vs `package.json`)
- AI-native tool references last verified: 2026-06-06 (still < 3 months)
- 2026-06-10: parallel security-hardening pass landed (5 fixes — see §2 Abuse / security lens); `test-plan-refresh-2026-06-10` change opened to re-ground + activate Phases 4–5 (R3/R4/R5).
- 2026-06-10 (refresh complete, `test-plan-refresh-2026-06-10`): re-grounded against the current suite (research in that change folder). Corrected spec counts (14→**39** unit, 17→**22** e2e) + the stale hot-spot citations (openrouter 156/30d is now the heaviest AI churn). R3 is now substantially unit-covered; R5 is partial (encryption + delete-flow done, row-gone + leak-scan missing). Recorded the **R4 code gap** (per-request size cap exists, but no repeat/loop guard — unmet by code, BYOK-mitigated) and the **default-skipped API-IDOR integration specs** (not in the `pnpm test` gate). Token HTTP API documented as **R1 by convergence** (operator decision), not a 9th risk. 3 of 5 hardening fixes now have regression tests. §1/§2 risk _scenarios_ unchanged; this was a freshness + grounding pass only.

- 2026-06-10: dropped the `editable-system-prompts` E2E from the test-debt backlog → recorded as a §7 deliberate won't-do. Rationale: user-editable free-text prompts have no fixed E2E oracle; preview≡builder correctness is already unit-covered. No risk scenario changed.
- 2026-06-10: closed the R4 **over-limit refusal** test gap → `src/__tests__/generate-caps.test.ts` (40th spec). Asserts notes (text/topic/PDF) + cards (topic/draft) reject one char past each per-request cap and never reach `generateObject`; mutation-checked by widening a cap (test fails as expected). The R4 **repeat/loop** dimension stays an open _code_ gap (no rate limiter; BYOK-mitigated) — not a test gap. Phase 4 §3 status literal unchanged (no dedicated change folder).

Refresh (`/10x-test-plan --refresh`) when:

- a new top-3 risk surfaces from the roadmap or archive,
- a recommended tool's `checked:` date is older than three months,
- the project's tech stack changes (new framework, new test runner),
- §7 negative-space no longer matches what the team believes,
- **S-19 Phase 2 (AI surface) lands** — Phases 4–5 move from gated to active and their risk anchors should be re-grounded by `/10x-research`.
